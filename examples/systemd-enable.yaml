targetConfigs:
- url: https://github.com/containers/fetchit
  systemd:
  - name: httpd-root
    targetPath: examples/systemd
    root: true
    enable: true
    schedule: "*/1 * * * *"
  branch: main

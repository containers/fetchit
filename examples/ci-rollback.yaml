targetConfigs:
- name: fetchit
  url: http://github.com/containers/fetchit
  raw:
  - name: raw
    targetPath: examples/ci-rollback
    schedule: "*/1 * * * *"
  rollback: true
  trackBadCommits: true
  branch: ci-rollback
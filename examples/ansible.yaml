targetConfigs:
- url: https://github.com/containers/fetchit
  ansible:
    - name: ansible
      targetPath: examples/ansible
      sshDirectory: /root/.ssh
      schedule: "*/1 * * * *"
  branch: main

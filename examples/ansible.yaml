volume: harpoon-volume # this needs to match the volume name passed to the podman run command
targets:
- name: harpoon
  url: http://github.com/cooktheryan/harpoon
  ansible: 
    targetPath: examples/ansible
    schedule: "*/1 * * * *"
  branch: ansible-v2

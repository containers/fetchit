volume: raw-volume # this needs to match the volume name passed to the podman run command
targets:
- name: fetchit
  url: http://localhost:8080/fetchit.zip
  disconnected: true
  methods:
    raw:
      targetPath: examples/raw
      schedule: "*/1 * * * *"
      pullImage: false
  branch: main

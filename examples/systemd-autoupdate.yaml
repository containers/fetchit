targetConfigs:
- name: autoupdate
  systemd:
  - name: autoupdate-ex
    autoUpdateAll: true
- name: fetchit
  url: http://github.com/containers/fetchit
  systemd:
  - name: sysd-ex
    targetPath: examples/systemd
    root: true
    enable: true
    schedule: "*/5 * * * *"
  branch: main

volume: fetchit-volume # this needs to match the volume name passed to the podman run command
targets:
- name: autoupdate
  methods:
    systemd:
      autoUpdateAll: true
# will update to containers/main after merge
- name: fetchit
  url: http://github.com/sallyom/harpoon
  methods:
    systemd:
      targetPath: examples/systemd/httpd.service
      root: true
      enable: true
      schedule: "*/5 * * * *"
  branch: podman-auto-update
